{% extends 'base.html.twig' %}

{% block body %}

    <section class="container-fluid">
        <div class="row">
            <div class="col-12">
                <h1>Hello world !!!</h1>
                <p>Number : <i>{{ number }}</i></p>
                <a href="{{ path('app_login') }}">Se connecter</a><br>
                <a href="{{ path('app_register') }}">S'enregistrer</a><br><br>
                <a href="{{ path('app_ucouleur') }}">U-Couleur</a>
            </div>
        </div>
    </section>

{% endblock %}