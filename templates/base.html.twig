<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}ULTRAGE{% endblock %}</title>
        <link rel="icon" href="{{ asset('img/favicon.png') }}">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {% if app.user %}
            <nav>
                <p>Bonjour {{ app.user.pseudo }} - <a href="{{ path('app_logout') }}">Déconnexion</a></p>
            </nav>
        {% endif %}
        {% block body %}{% endblock %}
    </body>
</html>
