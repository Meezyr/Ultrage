<aside class="sidebar d-flex flex-column flex-shrink-0 p-3 text-bg-dark">
    <a class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-white text-decoration-none" href="{{ path('app_accueil') }}">
        <img class="img-sidebar pe-none me-2" src="{{ asset('img/favicon.png') }}" alt="Logo Ultrage">
        <span class="fs-4">Ultrage</span>
    </a>
    <hr>
    <nav>
        <ul class="nav nav-pills flex-column mb-auto">
            <li class="nav-item">
                <a href="{{ path('app_ucouleur') }}" class="nav-link active" title="Accèder à U-Couleur"><i class="bi bi-palette pe-none me-2"></i> U-Couleur</a>
            </li>
        </ul>
    </nav>
    <hr>
    {% if app.user %}
        <div class="dropdown">
            <a href="#" class="d-flex align-items-center text-white text-decoration-none dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
{#                <img src="https://github.com/mdo.png" alt="" class="rounded-circle me-2" width="32" height="32">#}
                <strong>{{ app.user.pseudo|upper }}</strong>
            </a>
            <ul class="dropdown-menu dropdown-menu-dark text-small shadow">
                <li><a class="dropdown-item" href="{{ path('app_profile') }}">Profil</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li>
            </ul>
        </div>
    {% endif %}
</aside>
